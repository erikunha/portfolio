# deps
/node_modules

# next
/.next/
/out/

# production
/build

# env files
.env
.env*.local
!.env.example

# vercel
.vercel

# testing
/coverage
/coverage-report
/test-results
/playwright-report
/.playwright-results/

# local build artifact (next-build/ used by gates:runtime --skip-build)
/next-build

# lighthouse ci artifact reports
/.lighthouseci/

# local gates-runtime LHCI filesystem output
/.lhci-local/

# ai-eval harness output (scripts/ask-eval.ts)
/ask-eval-result.json

# local claude settings
.claude/settings.local.json

# playwright mcp session logs
.playwright-mcp/

tsconfig.tsbuildinfo

# review stamp — local signal that review agents passed for HEAD
.review-passed

# review findings ledger — transient per-cycle verification-loop state
.review-findings.json

# review findings archive — append-only learning-loop data (machine-local)
.review-findings-archive.jsonl

# learning-loop proposal inbox — SessionEnd hook appends here (machine-local)
.review-learnings.md

# WS4 API-edit security gate — session-local marker of unaudited API edits
.claude/.api-edit-pending

# git worktrees (superpowers using-git-worktrees skill creates these here)
.worktrees/

# os
.DS_Store

# git worktree symlinks (.claude/worktrees/ — created by superpowers using-git-worktrees)
.claude/worktrees/

# session-owned lock file — machine-generated runtime state, not source
.claude/scheduled_tasks.lock

# Semgrep local SARIF output
semgrep.sarif

# Stryker mutation testing output — transient sandbox + generated reports
.stryker-tmp/
mutation-report.html
mutation-report.json
