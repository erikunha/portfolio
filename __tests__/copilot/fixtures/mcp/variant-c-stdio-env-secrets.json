{
  "mcpServers": {
    "some-server": {
      "command": "npx",
      "args": ["some-mcp-server"],
      "env": {
        "API_KEY": "${SOME_API_KEY}",
        "REGION": "us-east-1"
      }
    }
  }
}
